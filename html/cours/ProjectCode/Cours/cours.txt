git clone "url" pour récupérer
git commit -m "nom" pour upload 
git add "chemin d'accée fichier" 

setup git 

git init
git add .
git commit -m "name"
git branch -M main
git remote add origin "url"
git push -u origin main

maj du repo 

git clone 'url'

dans les fichier : 

add les fichier récent dans le clone

git add .

git commit -m "name"